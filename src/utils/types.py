from enum import Enum, auto


class Screen(Enum):
    # Library
    # ------------------------
    ADD_SONG = auto()
    # ------------------------

    # Helpers
    # ------------------------
    MAIN = auto()
    BACK = auto()
    EXIT = auto()
    STAY = auto()
    TODO = auto()
    # ------------------------
